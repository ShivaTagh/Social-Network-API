/**
 * Exports all utility submodules
 * @module util
 */
module.exports = {
    autoloader: require('./autoloader'),
    noop: require('./noop'),
    assign: require('./assign'),
    factory: require('./factory')
};
